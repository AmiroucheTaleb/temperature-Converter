import { StyleSheet } from "react-native";

export const S = StyleSheet.create({
  button: {
    backgroundColor: "#19181b",
    borderColor: "white",
    borderWidth: 1.5,
    borderRadius: 25,
  },
  text: { color: "white", paddingHorizontal: 45, paddingVertical: 15 },
});
